{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "steps": {
    "install": {
      "commands": ["composer install --no-dev --optimize-autoloader --ignore-platform-reqs"]
    }
  }
}
